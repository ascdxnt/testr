# URL base de la aplicacion Ticketr (debe estar corriendo antes de ejecutar las pruebas)
base.url=http://localhost:8080

# Credenciales del usuario administrador por defecto de Ticketr
admin.email=admin@admin.com
admin.password=admin123

# Pausa (en milisegundos) entre pasos, solo para que la ejecucion en vivo se
# pueda seguir con la vista durante la presentacion. Poner en 0 para correr
# la suite a velocidad normal (por ejemplo, en una ejecucion automatizada).
demo.pause.ms=1200
